<h1>Edit Option</h1>
<%= simple_form_for @option, url: @url, :method => :patch do |f| %>
  <%= f.input :option_text, label: "Option", :required => true %>
  </br>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>